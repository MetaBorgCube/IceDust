module expressions-casts

imports

  signatures/-

rules
  
  [[ Cast(e1, t) ^ (s) : TTuple(t_type, e1_mult) ]] :=
    [[ e1 ^ (s) : TTuple(e1_type, e1_mult) ]],
    [[ t ^ (s) : t_type ]],
    CastType(t_type, e1_type) == None().
