module names-roles

language Relations

setup Common [[
	module test
	model
]]

test Duplicate role fails [[
	entity Node {
	}
	relation Edge{
		Node * in <- x
		Node * in
	}
]] 2 errors

test Resolve role from Navigator [[
	entity Node {
		name : String = concat(this > [[in]]]Edge[out .name)
	}
	relation Edge{
		Node + [[in]] <- x
		Node * out
	}
]] resolve #1 to #2

